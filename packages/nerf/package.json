{
  "name": "@plastiq/nerf",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Browser client for Plastiq's MLX NeRF/surface-capture service (services/nerf): submit posed images + transforms.json, poll, and receive a GLB that feeds the MeshDoc → reconstruct (mesh → B-rep) path.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@plastiq/ml": "workspace:*"
  }
}
