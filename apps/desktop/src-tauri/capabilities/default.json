{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Plastiq editor window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
